import { Box, Typography } from "@mui/material";

function Commit() {
    return (
        <Box padding={"15px 5px"}>
            <Typography>Bài đăng của bạn đã sẵng sàng để gửi !</Typography>
        </Box>
    );
}

export default Commit;
